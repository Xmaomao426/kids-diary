# 日记更新规则

## 触发条件与默认流程

- 当用户通过文字或语音转写提供新的胖胖日记，并明确要求“更新日记”或“新增日记”时，自动完成日记整理、数据更新、校验、审核、提交和推送。
- 用户通过 Codex Remote 语音输入时，语音转写文本视为原始输入，不要求用户按固定格式表达。例如：“更新今天的日记，胖胖早上九点多醒，早饭吃了鸡蛋和粥……”
- 对普通日记更新，默认完成以下完整闭环：
  1. 更新日记。
  2. 运行现有校验。
  3. 审核 `git diff`。
  4. 创建清晰的 commit。
  5. 推送到 `main`。
  6. 检查 GitHub Pages 部署结果。

## 默认分类

优先沿用当前六类：

1. 作息
2. 饮食
3. 活动 / 出行
4. 健康 / 护理
5. 行为 / 情绪
6. 家庭 / 事件

这六类是当前默认分类，不是永久封死的分类体系：

- 新内容能够合理归入现有类别时，不新增分类。
- 如果确实出现无法合理归类、且具有长期独立价值的新类型，不得硬塞进现有类别。应先向用户提出“新增分类建议”，说明原因和建议名称，等待用户确认后，再修改数据结构、页面和校验规则。
- 未经用户确认，不得自行新增第七类或更多分类。

## 日记整理原则

- 先将原始输入拆成原子事件，再逐项分类；同一句包含多个事件时必须拆分。
- 不得编造原文不存在的信息。可以调整标点和语序以提高可读性，但不得改变事实含义。
- 必须分清人物主体，孩子事件与家长、祖辈及其他家庭成员事件不得混淆。
- 日期、时间、数量、饮食、排便、睡眠等具体信息应尽量保留。
- 存在可能改变事实的歧义时，必须询问用户，不得猜测。
- 轻微口语或语音转写错误，如果能够根据上下文高置信判断，可以规范表达；如果修正可能改变事实，必须先请用户确认。

## 日期、合并与修正

- 根据用户明确提供的日期更新对应记录。
- 用户使用“今天”“昨天”等相对日期时，先按执行任务时的当前本地日期解析，再更新对应记录。
- 对当天已有记录，以合并为主，不覆盖已有有效记录。
- 自动去除完全重复的事件。
- 不得因新增内容删除原有事实，除非用户明确要求修正或删除。

## 校验与发布门禁

- 更新完成后必须运行 `scripts/validate_diary.py`。
- 校验失败时不得 commit、不得 push；应先修复问题并重新校验。
- 同时检查页面 JavaScript 和数据结构是否正常。
- 提交前审核 `git diff`，确认修改仅限本次日记更新所需内容。
- 校验和审核通过后，创建 commit、推送到 `main`，并检查 GitHub Pages 部署结果。

## 普通日记更新的范围限制

除非用户明确要求，否则一次普通日记更新不得：

- 修改页面视觉。
- 重构项目。
- 调整分类体系。
- 修改 GitHub Actions。
- 修改安全策略。
- 新增无关文件。

## 完成报告

任务完成后只简要报告：

- 更新日期。
- 新增或修改了哪些事件。
- 是否存在需要用户确认的歧义。
- 校验结果。
- commit hash。
- push 结果。
- GitHub Pages 部署结果。
